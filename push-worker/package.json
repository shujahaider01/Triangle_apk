{
  "name": "triangle-push-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Drains Firebase RTDB /pushQueue and sends the real FCM push each entry represents",
  "main": "drain.js",
  "scripts": {
    "start": "node drain.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
