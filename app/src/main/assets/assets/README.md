# assets/

Place any static assets here:

- **icons/**    — app icon images (favicon, PWA icons)
- **fonts/**    — custom web fonts (if any added later)
- **images/**   — any logo or illustration files

Currently the portal uses no external assets —
everything is inline CSS/JS with emoji icons.
