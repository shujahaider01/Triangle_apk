<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Self-hosted in-app updates (update/AppUpdater.kt) hand the downloaded APK to the system installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- Reminder feature: exact-time task/habit alarms (see reminders/ReminderScheduler.kt)
         and re-arming them after a reboot clears every pending AlarmManager alarm. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TraineeXP"
        android:enableOnBackInvokedCallback="true">
        <!-- Opts the whole app into Android's Predictive Back system — this
             is what makes the edge-swipe gesture (and the button) route
             through MainActivity's OnBackPressedCallback consistently
             (registerBackHandling()), on gesture-nav devices including
             Xiaomi MIUI/HyperOS. Without this flag, gesture navigation can
             behave inconsistently with the legacy back-button-only path. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Phase 2 — receives every FCM push and builds the system
             notification (see TxpMessagingService.kt for why this has to
             happen in code rather than relying on FCM's automatic display). -->
        <service
            android:name=".TxpMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Phase 1 — FCM push notifications: default channel/icon/color used
             whenever a push arrives without an explicit channel/icon set
             (the actual NotificationChannel object gets created in Phase 2's
             Kotlin code — this just points Firebase's default handling at it). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- Reminder feature: fires when an AlarmManager alarm scheduled by
             ReminderScheduler goes off, and re-arms every pending reminder
             after a reboot (AlarmManager alarms don't survive one). -->
        <receiver
            android:name=".reminders.ReminderAlarmReceiver"
            android:exported="false"/>
        <receiver
            android:name=".reminders.ReminderBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Lets CameraCapture.kt hand the system camera app a content:// Uri
             for its output instead of a raw file:// path (required since API 24). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
